<OL>
% while (my $article = $articles->Next()) {
<LI><a href="article.html?id=<%$article->Id%>"><% $article->Summary %></a>

% if (0) {
&nbsp;&nbsp;&nbsp&nbsp;
	 <a href="edit/edit_article.html?id=<%$article->Id%>">[edit]</a>
	 <a href="edit/create_article.html?Parent=<%$article->Id%>">[new child]</a>
% }
% if ($Depth > 0) {
<& ArticleHierarchy, parent => $article->Id , Depth => $Depth &> 
% }
</LI>
% }
</OL>
<%INIT>
# Subtract 1 from depth. by default, depth 0, means unlimited.
$Depth--;
my $articles = new RT::FM::ArticleCollection($session{'CurrentUser'});
$articles->LimitToParent($parent);
</%INIT>
<%ARGS>
$parent => 0
$Depth => 0
</%ARGS>
