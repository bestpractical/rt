language: bash
services: docker

before_install:
  - docker build -t rt44 .
  - docker run -d --name rt rt44
  - docker ps -a

script:
    - docker exec -it rt bash -c "cd /usr/local/rt && prove -l t/*"
