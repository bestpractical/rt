1. all the method names must use foo_bar, so do method arguments
2. config vars can be both FooBar or foo_bar, we should support both, i.e. when
calling ->get or ->set, we can use both conventions. convention saved in file is
FooBar. 

