%# $Header$

<SELECT NAME ="<%$Name%>">
<OPTION VALUE="">-</OPTION>
% while (my $queue=$q->Next) {
<OPTION VALUE="<%$queue->Id%>" <%($queue->Id == $Default) && 'SELECTED'%>><%$queue->QueueId%>
%   if (($Verbose) and ($queue->Description) ){
(<%$queue->Description%>)
%  }
</OPTION>
% }
</SELECT>

<%ARGS>
$Name => undef
$Verbose => undef
$Default => undef
</%ARGS>

<%INIT>
use RT::Queues;
my $q=new RT::Queues($session{'CurrentUser'});
$q->UnLimit;
</%INIT>
