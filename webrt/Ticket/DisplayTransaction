<%ARGS>
$Transaction => undef
$attachments => $Transaction->Attachments

</%ARGS>

Date: <%$Transaction->CreatedAsString%> 

% if ($Transaction->TimeTaken > 0) {
(<%$Transaction->TimeTaken%> minutes)
% }
<%$Transaction->Description%>
<br>

%  while (my $message=$attachments->Next) {

%# TODO: This is not really the right way to do it.  
% # The if (0) stuff below seems a bit more correct
% # .. though still a lot of work is needed.
<a href="Update.html?id=<%$Transaction->Ticket%>&QuoteTransaction=<%$message->Id%>">Act</a>
<h3><%$message->Subject%></h3>
<hr>
<pre>
<%$message->Content%>
</pre>

% if (0) { 

<hr width=60%>
<pre>
<%$message->Headers%>
</pre>

%   if ($message->ContentType =~ m{^(text/plain|message)}) {
<pre>
<%$message->Content%>
</pre>
%    } 
% else {
	
<% $message->ContentType %> not shown
%    }
% }
% }



