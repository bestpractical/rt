%while (my $Transaction = $Ticket->Transactions->Next) {
% $FullHeaders=1 if ($ShowHeaders eq $Transaction->Id);
%	if ($Ticket->Transactions->IsLast) {
	<a name="lasttrans">
% 	}
	<& ShowTransaction, Transaction => $Transaction, FullHeaders => $FullHeaders &>
<BR>
%}


<%ARGS>
$Ticket => undef
$ShowHeaders => undef
$FullHeaders => undef
</%ARGS>
